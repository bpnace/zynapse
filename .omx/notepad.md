

## MANUAL
User preference: In all future German copy, never replace umlauts with ae/oe/ue. Always write ä/ö/ü unless ASCII-only text is explicitly required (e.g. code, env vars, identifiers, URLs, or user-requested transliteration).
