# Ralph Context Snapshot

- task statement: Implement only the Phase 1 technical foundation for the brand workspace demo.
- desired outcome: Supabase/Drizzle groundwork, protected workspace shell, auth callback, env handling, minimal schema, and tenant-scoped auth foundation are present and verified.
- known facts/evidence:
  - Repo uses `pnpm`, `Next.js 16`, `React 19`, App Router.
  - No existing auth, database, or workspace code is present.
  - Approved checklist and architecture artifacts already exist under `.omx/plans/`.
  - Supabase project is available and the `public` schema is currently empty.
- constraints:
  - No buyer workflows or seed data implementation yet.
  - No new libraries except roadmap-approved Supabase + Drizzle stack.
  - Keep package manager as `pnpm`.
  - Use apply_patch for edits.
- unknowns/open questions:
  - Exact invite transport UX through Supabase email flow will need later integration details.
  - Whether middleware-only session refresh is sufficient versus extra layout-level checks will be proven during verification.
- likely codebase touchpoints:
  - `package.json`
  - `.env.example`
  - `src/lib/env.ts`
  - `src/app/login/page.tsx`
  - `src/app/layout.tsx`
  - new `src/app/(workspace)/*`
  - new `src/lib/auth/*`
  - new `src/lib/db/*`
  - new `middleware.ts`
